import React from "react";
import Hero from "../components/Hero";

const Landing = () => {
  return (
    <main className="main-content min-h-screen flex flex-col items-center justify-center">
      <Hero />
    </main>
  );
};

export default Landing;
